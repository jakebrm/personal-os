// Catches /dashboard/health, /dashboard/health/sleep, etc.
// Auth is enforced by middleware for all /dashboard/* paths.
// Shell is rendered by the layout so it survives tab navigation.
export default function DashboardSlugPage() {
  return null;
}
